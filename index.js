/**
 * @format
 */

import {AppRegistry} from 'react-native';
import Gallery from './src/navigation/Imagepicker';
import {name as appName} from './app.json';

AppRegistry.registerComponent(appName, () => Gallery);
